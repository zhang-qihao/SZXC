/**
 * @hidden
 */
export function getDefaultSlots(component: any) {

    const defaultSlots = component.$slots.default;

    return (component.v3 && defaultSlots && typeof defaultSlots === 'function') ? defaultSlots() : defaultSlots;
}
