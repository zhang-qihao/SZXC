import Vue from 'vue';
import { Push } from '../src/main';

Vue.component('Push', Push);
let a = new Vue({
    el: '#app',
    data: function () {
        return {
            direction: 'left',
            index: 1
        };
    },
    methods: {
        onClick: function() {
            this.index = this.index + 1;
        }
    }
});
