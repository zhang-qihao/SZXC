import Vue from 'vue';
import { Slide } from '../src/main';

Vue.component('Slide', Slide);
let a = new Vue({
    el: '#app',
    data: function () {
        return {
            show: true
        };
    },
    methods: {
        onClick: function() {
            this.show = !this.show;
        }
    }
});
