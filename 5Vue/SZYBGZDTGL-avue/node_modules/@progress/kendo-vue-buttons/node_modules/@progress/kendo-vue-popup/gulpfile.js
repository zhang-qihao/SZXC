'use strict';

const gulp = require('gulp');
const path = require('path');
const compilerPath = path.join(__dirname, '../../', 'node_modules', '.bin', 'tsc');

require('@telerik/kendo-vue-tasks')(gulp, 'kendo-vue-popup', compilerPath, __dirname);
